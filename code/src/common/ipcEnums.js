export const NUM_POSITIONS = 4;
// all indices must be less than this, all values must be less than 2^32

export const INDEX_WAITING = 0;
export const STATUS_WAITING = 0;
export const STATUS_READY = 1;

export const INDEX_QUEUE = 1;
export const STATUS_DATA = 0;
export const STATUS_FAIL = 1;
export const STATUS_DONE = 2;
