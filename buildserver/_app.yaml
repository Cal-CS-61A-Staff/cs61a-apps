runtime: custom
service: buildserver
env: flex

env_variables:
  ENV: "prod"

handlers:
- url: /.*
  script: auto
  secure: always

beta_settings:
  cloud_sql_instances: cs61a-140900:us-west2:cs61a-apps

inbound_services:
- warmup
